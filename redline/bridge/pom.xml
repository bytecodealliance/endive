<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>run.endive</groupId>
    <artifactId>redline-parent-experimental</artifactId>
    <version>999-SNAPSHOT</version>
    <relativePath>../pom.xml</relativePath>
  </parent>
  <artifactId>redline-bridge-experimental</artifactId>
  <packaging>jar</packaging>
  <name>Endive - Redline Bridge</name>
  <description>Java wrapper for the Cranelift code generator (compiled to Wasm)</description>

  <dependencies>
    <dependency>
      <groupId>run.endive</groupId>
      <artifactId>runtime</artifactId>
    </dependency>
    <dependency>
      <groupId>run.endive</groupId>
      <artifactId>wasi</artifactId>
    </dependency>
    <dependency>
      <groupId>run.endive</groupId>
      <artifactId>wasm</artifactId>
    </dependency>
  </dependencies>

  <build>
    <!--
      Uses exec:java with Generator.main() instead of endive-compiler-maven-plugin
      to avoid a circular dependency: the plugin depends on redline-compiler which
      depends on this bridge module. exec:java with build-time-compiler as a plugin
      dependency breaks the cycle.
    -->
    <plugins>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <version>${build-helper-maven-plugin.version}</version>
        <executions>
          <execution>
            <id>add-generated-sources</id>
            <goals>
              <goal>add-source</goal>
            </goals>
            <phase>generate-sources</phase>
            <configuration>
              <sources>
                <source>${project.build.directory}/generated-sources/endive-compiler</source>
              </sources>
            </configuration>
          </execution>
          <execution>
            <id>add-generated-resources</id>
            <goals>
              <goal>add-resource</goal>
            </goals>
            <phase>generate-resources</phase>
            <configuration>
              <resources>
                <resource>
                  <directory>${project.build.directory}/generated-resources/endive-compiler</directory>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <dependencies>
          <dependency>
            <groupId>run.endive</groupId>
            <artifactId>build-time-compiler</artifactId>
            <version>${project.version}</version>
          </dependency>
        </dependencies>
        <executions>
          <execution>
            <id>redline-bridge-compile</id>
            <goals>
              <goal>java</goal>
            </goals>
            <phase>generate-sources</phase>
            <configuration>
              <mainClass>run.endive.build.time.compiler.GeneratorMain</mainClass>
              <arguments>
                <argument>${project.basedir}/../cranelift_bridge.wasm</argument>
                <argument>run.endive.redline.experimental.bridge.internal.Cranelift</argument>
                <argument>${project.build.directory}/generated-resources/endive-compiler</argument>
                <argument>${project.build.directory}/generated-sources/endive-compiler</argument>
                <argument>${project.build.directory}/generated-resources/endive-compiler</argument>
                <argument>WARN</argument>
                <argument>run.endive.redline.experimental.bridge.internal.CraneliftBridge</argument>
              </arguments>
              <includePluginDependencies>true</includePluginDependencies>
              <includeProjectDependencies>false</includeProjectDependencies>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
